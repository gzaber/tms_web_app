name: task
description: Task API package.
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  auth:
    path: .././auth
  http: ^0.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.16
  mongo_dart: ^0.7.4
