name: tms_web_app
description: Task Management System.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  auth:
    path: ./auth
  task:
    path: ./task
  equatable: ^2.0.3
  flutter_bloc: ^8.0.0
  google_fonts: ^2.1.0
  intl: ^0.17.0
  flutter_dotenv: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.16
  bloc_test: ^9.0.1

flutter:
  uses-material-design: true
  assets:
    - .env
